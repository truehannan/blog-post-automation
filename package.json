{
  "name": "blog-automation",
  "version": "1.0.0",
  "description": "AI-powered blog automation for tech/finance content",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "format": "prettier --write src/**/*.ts"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240919.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "wrangler": "^3.38.0",
    "prettier": "^3.1.0"
  }
}
